# Пример файла окружения. Скопируйте в .env: cp .env.example .env
# Путь к файлу базы данных SQLite (относительно корня приложения)
DATABASE_URL="file:./prisma/dev.db"

# Секрет для подписи cookie-сессий. В реальном развёртывании замените на свой.
SESSION_SECRET="change-me-to-a-long-random-secret"
